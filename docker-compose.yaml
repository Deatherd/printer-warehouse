version: '3.8'

services:
  app:
    build: .
    container_name: printer_inventory
    ports:
      - "5000:5000"
    volumes:
      - printer_data:/app/data
      - ./templates:/app/templates
    env_file:
      - warehouse.env
    restart: unless-stopped

volumes:
  printer_data:
    driver: local